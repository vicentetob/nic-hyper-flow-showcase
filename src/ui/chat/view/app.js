/**
 * LEGACY FILE - DEPRECATED
 * 
 * Este arquivo foi substituído por app.ts e features modulares.
 * Mantido apenas como stub para evitar erros durante transição.
 * 
 * TODO: Remover após confirmação de que app.ts está funcionando corretamente.
 */

console.warn('[DEPRECATED] app.js está sendo carregado. Use app.ts (compilado) em vez disso.');

// Stub mínimo para evitar crashes
if (typeof acquireVsCodeApi !== 'undefined') {
  const vscode = acquireVsCodeApi();
  
}
